import java.util.Scanner;

public class vagner {
    public void main(String[] args) {

        Scanner teclado = new Scanner(System.in);

        /*
         * Exemplo com Retângulo:
         * System.out.println("Digite dois valores (Altura e Largura):");
         * double x = teclado.nextDouble();
         * double y = teclado.nextDouble();
         * Retangulo r1 = new Retangulo(x, y);
         * r1.toString();
         */

        /*
         * Exemplo com Empregado:
         * System.out.println("[NOME] [SALÁRIO BRUTO] [DESCONTO]");
         * String n = teclado.nextLine();
         * double s = teclado.nextDouble();
         * double d = teclado.nextDouble();
         * Empregado douglasMizzuno = new Empregado(n, s, d);
         * 
         * douglasMizzuno.aumentarsalario(50);
         * douglasMizzuno.aumentarsalario(150);
         * douglasMizzuno.salarioliquito();
         */

        teclado.close();
    }
}
